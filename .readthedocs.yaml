version: 2
build:
  os: ubuntu-22.04

  tools:
    python: "3.11"

  apt_packages:
    - libpango1.0-dev

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs

sphinx:
  configuration: docs/conf.py

formats: all

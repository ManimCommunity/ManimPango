version: 2
build:
  os: ubuntu-22.04

  tools:
    python: "3.11"

  apt_packages:
    - libpango1.0-dev

python:
   install:
      - requirements: docs/requirements.txt
      - method: pip
        path: .

sphinx:
  configuration: docs/conf.py

formats: all
